- type: filestream
  id: syslog_input
  enabled: true
  paths:
    - /var/log/syslog
  fields:
    kafka_topic: "system_syslog_log"
  fields_under_root: true
  processors:
    - add_host_metadata: ~
    - include_fields:
        fields: ["host.name", "message", "kafka_topic"]
