- type: filestream
  id: kern_input
  enabled: true
  paths:
    - /var/log/kern.log
  fields:
    kafka_topic: "system_kern_log"
  fields_under_root: true
  processors:
    - add_host_metadata: ~
    - include_fields:
        fields: ["host.name", "message", "kafka_topic"]
